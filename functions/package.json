{
  "name": "tengaloans-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for TengaLoans",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0",
    "nodemailer": "^7.0.12",
    "sharp": "^0.32.6",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/nodemailer": "^7.0.4",
    "typescript": "^5.3.2"
  },
  "private": true
}
